name: My first Flow
on:
  push:
    branches:
      - master
  workflow_dispatch:
  
jobs:
  MyFirstJob:
    runs-on: ubuntu-latest
    steps:
    - run: echo "👋 Hello World!"

    - name: Checkout Repo
      uses: actions/checkout@v2

    - name: List files in the repo
      run: |
         echo "folloing are ${{github.repository}} file tree:"
         tree