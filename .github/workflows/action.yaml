name: My first
on: 
  push: 
    branches: 
    - master
  workflow_dispatch:

jobs: 
  MyfirstJob:
    runs-on: ubuntu-latest

    steps: 
      - run: eco "👋 Hello World!"

      - name: Checkout Code
        run: actions/checkout@v3.0

      - name: list files in repo
        run: |
          echo "The repor ${{github.repository}}" contains following files:"
          tree